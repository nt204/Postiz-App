# === node_modules — không copy vào build context, tiết kiệm hàng trăm MB
**/node_modules

# === build artifacts — sẽ được build lại trong container
**/dist
**/.next
.nx

# === secrets — KHÔNG BAO GIỜ copy vào image
.env
.env.*
!.env.example

# === git
.git
**/.git
.github

# === dev tools & editor
.devcontainer
**/.vscode
**/*.vscode
**/*.idea

# === logs
**/npm-debug.log
**/yarn-error.log
**/pnpm-debug.log

# === test & coverage
**/coverage
**/__tests__
**/*.test.ts
**/*.spec.ts

# === docs & misc
**/*.md
**/LICENSE
reports
docker-data

# === local upload data (được mount qua Docker volume)
uploads